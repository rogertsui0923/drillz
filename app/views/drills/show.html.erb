<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Code Core Drillz | May the Force be with You</title>
    <!-- <link rel="stylesheet" href="css/bootstrap.min.css"> -->
    <link rel="stylesheet" href="/stylesheets/drills">
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a>Logo</a></li>
          <li><a>Drillz</a></li>
          <li><a>Leaderboard</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a>Hello, Basia</a></li>
            <li><a>Profile</a></li>
            <li><a>Sign Out</a></li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
    </nav>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
          <h1>Drill Group: <%= @drill.drill_group.name %></h1>
            <p><%= @drill.drill_group.description %></p>
        </div>
      </div>
    </div>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
          <h3>Answers</h3>
          <%= simple_form_for [@drill,@solution],
              url: drill_attempts_path(@drill),
              method: :post,
              html: { class: 'answer-form' } do |f| %>
             <%= f.input :body, input_html: {class: 'form-group'} %>
             <%= f.submit :submit, class: 'btn btn-primary' %>
          <% end %>
        </div>
      </div>
    </div>
    <hr>

    <% if @drill.next.nil? %>
      <%= button_to 'Done!', group_session_path(@group_session),
                             method: :get,
                             class: 'btn btn-primary' %>
    <% else %>
      <%= button_to 'NEXT DRILL', drill_path(@drill.next),
                                  method: :get,
                                  class: 'btn btn-primary' %>
    <% end %>

    <div class="container-fluid"> <!-- Main post container -->
      <div class="row">
        <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
          <p class="success-message"><%= alert || notice %></p>
          <% @drill.solutions.each do |s| %>
          <div class="submitted-answer">
            <p> <%= s.body %></p>
          </div>
          <% end %>
        </div>
      </div>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
      <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="js/bootstrap.min.js"></script>
  </body>
</html>
