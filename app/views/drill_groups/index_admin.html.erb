<!-- index view for admin -->

<%= link_to 'Add Group', new_drill_group_path %>

<h1>Drill Groups</h1>

  <% @drill_groups.each do |drill_group| %>
    <div>
      <%= drill_group.name %>
      <%= link_to 'View', drill_group %>
      <%= link_to 'Edit', edit_drill_group_path(drill_group) %>
      <%= link_to 'Delete', drill_group, method: :delete %>
    </div>
  <% end %>
